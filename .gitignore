# macOS
.DS_Store

# Xcode — dérivés de build
build/
DerivedData/
*.moved-aside
*.pbxuser
!default.pbxuser
*.mode1v3
!default.mode1v3
*.mode2v3
!default.mode2v3
*.perspectivev3
!default.perspectivev3

# Xcode — état utilisateur (spécifique à chaque poste)
xcuserdata/
*.xcuserstate
*.xccheckout

# Swift Package Manager
.swiftpm/
.build/
# Package.resolved est volontairement VERSIONNÉ (épingle Sparkle à une version précise).

# Dépendances tierces (au cas où)
Pods/
Carthage/Build/

# Instructions personnelles Claude Code (privées)
CLAUDE.local.md

# Divers
*.log
